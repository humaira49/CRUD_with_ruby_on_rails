<head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<%= csrf_meta_tags %>
</head>
<!--________________________displaying data in list_____________________
<h1>Users</h1>
<ul>
  <% @info.each do |info| %>
    <li>
      <%= info.name %>
    </li>
    <li>
    <%= info.lastname %>
  </li>
  <li>
  <%= info.message %>
</li>
  <% end %>
</ul>-->
<!-- ___________________________________in__table____________________-->
<h1>Users</h1>
<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Lastname</th>
      <th>Message</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>
  <tbody>
    <% @info.each do |info| %>
      <tr>
        <!--<td><%= info.id %></td>-->
        <td><%= info.name %></td>
        <td><%= info.lastname %></td>
        <td><%= info.message %></td>
        <td>
        <%= link_to 'Edit 🖋', welcome_path(info)%>
         </td>
         <td>
        <%= link_to '  Delete 🗑', delete_welcome_path(info), method: :delete, data: { confirm: 'Are you sure?' } %>
      </td>
      </tr>
    <% end %>

  </tbody>
</table>
<hr>
<!--__________________________________________inserting data___________________-->
<h1>Add new user</h1>
<%= form_tag '/users' do %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>


  <div class="form-group">
    <label for="user_name">Name</label>
    <%= text_field_tag 'name', nil, class: 'form-control', id: 'user_name' %>
  </div>

  <div class="form-group">
    <label for="user_lastname">Lastname</label>
    <%= text_field_tag 'lastname', nil, class: 'form-control', id: 'user_lastname' %>
  </div>

  <div class="form-group">
    <label for="user_message">Message</label>
    <%= text_area_tag 'message', nil, class: 'form-control', id: 'user_message' %>
  </div>

  <div>
    <%= submit_tag 'Save', class: 'btn btn-primary', data: { disable_with: 'Saving...' } %>
  </div>
<% end %>
